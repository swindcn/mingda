{
  "name": "mingda-casting",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:admin": "npm --prefix apps/admin run dev",
    "dev:api": "npm --prefix apps/api run dev",
    "build:admin": "npm --prefix apps/admin run build",
    "build:api": "npm --prefix apps/api run build",
    "build:miniprogram": "npm --prefix apps/miniprogram run build",
    "typecheck:miniprogram": "npm --prefix apps/miniprogram run typecheck",
    "lint:admin": "npm --prefix apps/admin run lint",
    "preview:admin": "npm --prefix apps/admin run preview",
    "prisma:generate": "npm --prefix apps/api run prisma:generate",
    "prisma:migrate": "npm --prefix apps/api run prisma:migrate",
    "test:permissions": "node scripts/test-permissions.mjs"
  }
}
