{
  "name": "@mingda/miniprogram",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^4.0.5",
    "typescript": "~6.0.2"
  }
}
