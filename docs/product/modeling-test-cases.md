# 生产建模模块测试用例

更新时间：2026-08-13

## 测试范围

覆盖生产建模模块的后端权限、真实 Prisma relation、多值明细表映射、引用删除限制和基础 CRUD。

## 前置条件

- API 服务可访问：`http://124.223.2.193/api`
- 管理员账号：`admin / 13665068911`
- PostgreSQL 已执行最新 Prisma schema。
- 系统中至少有一个内部员工可用于班组成员测试。

## 用例清单

| 编号 | 模块 | 场景 | 步骤 | 预期 |
|---|---|---|---|---|
| MDM-001 | 鉴权 | 管理员登录 | 使用管理员账号登录 | 返回 token |
| MDM-002 | 权限 | 无建模权限用户访问建模接口 | 创建普通员工并登录，请求 `/admin/modeling/items` | 返回 403 |
| MDM-003 | 车间 | 新增车间 | 新增测试车间 | 返回成功，列表可查 |
| MDM-004 | 产线 | 外键引用有效车间 | 新增产线并绑定测试车间 | 返回成功 |
| MDM-005 | 产线 | 外键引用无效车间 | 新增产线绑定不存在车间 | 返回 400 |
| MDM-006 | 班组 | 班组成员明细表 | 新增班组并绑定内部员工 | 详情返回 `memberUserIds` |
| MDM-007 | 设备 | 熔炼配方炉型选项 | 新增熔炼车间启用设备和非熔炼设备，读取配方选项 | 仅返回熔炼车间启用设备 |
| MDM-008 | 物料/配方 | 熔炼配方明细表 | 选择原材料物料新增草稿配方并添加配料明细 | 详情返回目标成分、炉型和配料明细 |
| MDM-015 | 材质牌号 | 质量标准明细 | 新增牌号并维护元素、力学性能、工艺要求、标准版本 | 详情返回四类明细；重复明细名称被拒绝 |
| MDM-016 | 材质牌号关联 | 删除约束 | 创建配方引用材质牌号后删除牌号，再清理配方后删除 | 被配方/设备/物料引用时拒绝删除，解除引用后可删除 |
| MDM-009 | 模具/芯盒 | 模具和芯盒真实关联 | 新增模具并新增芯盒绑定该模具 | 返回成功 |
| MDM-010 | 班次/日历 | 工厂日历启用班次明细表 | 新增班次、新增日历并绑定班次 | 详情返回 `shiftCodes` |
| MDM-011 | 排班 | 动态排班真实关联 | 新增排班绑定车间、班次、班组 | 返回成功 |
| MDM-012 | 工艺路线 | 可视化拓扑真实关联 | 新增路线，拖入工序、连接节点并绑定设备 | 返回节点、设备关系和有向边 |
| MDM-013 | 删除限制 | 被引用车间不能删除 | 产线/班组/排班存在时删除车间 | 返回 400 |
| MDM-014 | 清理 | 按依赖反向删除测试数据 | 删除排班、日历、芯盒、模具、路线、配方、设备、班组、产线、车间等 | 全部清理成功 |
| MDM-017 | 配方 | 材质成分自动带入 | 新建配方并选择启用材质牌号 | 自动生成该牌号的化学成分快照，允许编辑 |
| MDM-018 | 配方 | 原材料范围 | 读取配方选项并尝试提交非原材料物料 | 选项仅含“原材料”一级类型，后端拒绝其他物料 |
| MDM-019 | 配方 | 比例与用量 | 基准重量 1000kg，原材料比例分别填写 30%、40%、30% | 标准用量分别为 300kg、400kg、300kg，允许提交生效 |
| MDM-020 | 配方 | 生效校验 | 原材料与回炉料比例合计不为 100% 后提交生效 | 返回明确校验错误，配方仍为草稿 |
| MDM-021 | 配方 | 克隆 | 对任一配方执行复制 | 新编码不含 Copy；名称追加“-副本”；版本 V1.0；状态草稿；全部明细保留 |
| MDM-022 | 配方 | 状态机 | 草稿提交生效，再停用，并尝试编辑/删除 | 状态依次为草稿、已生效、已停用；非草稿不能编辑或删除 |
| MDM-023 | 配方权限 | 业务按钮权限 | 分别配置 view、clone、activate、disable 权限 | 菜单、按钮和后端接口均按实际授权生效 |
| MDM-024 | 配方版本 | 已生效禁止修改 | 对 ACTIVE 配方直接提交编辑 | 后端拒绝并提示先停用 |
| MDM-025 | 配方版本 | 停用修改升级 | 将 V1.0 配方停用后编辑保存 | 编码不变，版本变为 V2.0，状态回到草稿 |
| MDM-026 | 配方版本 | 草稿重复保存 | 对 V2.0 草稿连续编辑保存 | 版本保持 V2.0，不重复升级 |
| MDM-027 | 配方版本 | 连续版本升级 | V2.0 重新生效、停用并保存修改 | 版本变为 V3.0，状态回到草稿 |
| MDM-028 | 铸造 BOM | 新建首版本 | 选择成品、材质、重量参数和合法物理用料保存 | 创建该产品唯一 BOM 主档和 V1.0 草稿，刷新后仍存在 |
| MDM-029 | 铸造 BOM | 重量自动计算 | 净重 45kg、浇注毛重 65kg 保存 | 后端保存收得率 69.2308%、单件回料 20kg |
| MDM-030 | 铸造 BOM | 物理用料范围 | 分别选择砂芯、铸造辅材、工装耗材及原材料 | 前三类可提交；原材料不在选项且后端拒绝 |
| MDM-031 | 铸造 BOM | 唯一生效与生命周期并发 | V1.0 生效，创建 V2.0 后再生效；持有 BOM advisory lock 时并发发起草稿编辑/删除 | V2.0 为已生效，V1.0 自动变为已停用；草稿写入必须等待同一锁释放并在锁内重验状态 |
| MDM-032 | 铸造 BOM | 新版本复制 | 基于已生效或已停用版本创建新版本 | 生成下一 `Vn.0` 草稿，重量、材质和明细完整复制 |
| MDM-033 | 铸造 BOM | 跨产品克隆 | 将来源 BOM 克隆到另一个已有成品/半成品 | 目标产品生成草稿版本并记录来源；不创建新物料 |
| MDM-034 | 铸造 BOM | 克隆目标限制 | 目标选择原产品或原材料 | 后端分别提示使用新版本功能、目标物料类型不合法 |
| MDM-035 | 铸造 BOM | 生产需求计算 | 对毛重 65kg 的 BOM 计算 100 件，砂芯单耗 1、损耗 2% | 铁水 6500kg、回料 2000kg、砂芯需求 102 个 |
| MDM-036 | 铸造 BOM | 材质配方关联 | BOM 选择已有生效配方的材质后查看详情和计算接口 | 展示同材质已生效配方摘要，但 BOM 不固定绑定配方编码 |
| MDM-037 | 铸造 BOM | 状态机限制 | 尝试编辑/删除已生效或已停用版本 | 后端拒绝；按钮按状态隐藏，只允许规定动作 |
| MDM-038 | 铸造 BOM 权限 | 菜单与按钮权限 | 分别配置 view/create/edit/delete/clone/activate/disable/new_version | 菜单、页头新增、操作按钮及后端接口按实际授权生效 |
| MDM-039 | 铸造 BOM | 数据归属 | 角色分别配置本人、部门、自定义部门、全部及同步公共数据范围 | 列表和详情按 `modeling:boms` 归属过滤，越权详情返回不存在 |
| MDM-040 | 铸造 BOM | 并发版本与生效 | 同时创建两个新版本，再同时提交生效 | 顺序生成 V3.0/V4.0，最终仅一个版本为已生效 |
| MDM-041 | 铸造 BOM | 来源版本追溯 | 从草稿克隆目标产品后删除来源草稿 | 后端拒绝删除，来源版本关系保持完整 |
| MDM-042 | 铸造 BOM | 新版本来源限制 | 对草稿直接调用创建新版本接口 | 后端拒绝，仅已生效或已停用版本可创建新版本 |
| MDM-043 | 铸造 BOM | 业务归属原子性 | 创建首版或复制版本后查询归属记录 | BOM 版本与 `modeling:boms` 归属在同一事务内同时存在 |
| MDM-044 | 铸造 BOM 工装 | 模具选项范围 | BOM 打开生产模具下拉 | 显示全部启用模具档案及关联物料信息，不显示停用模具 |
| MDM-045 | 铸造 BOM 工装 | 芯盒联动 | 选择一个或多个生产模具 | 自动带入已选模具下的启用芯盒，且用户可以手动移除或补选 |
| MDM-046 | 铸造 BOM 工装 | 后端关系校验 | 提交其他物料关联的有效模具、未选所属模具的芯盒 | 前者允许保存；后者返回芯盒必须属于已选模具 |
| MDM-047 | 铸造 BOM 工装 | 同产品版本复制 | 创建新版本后查看详情 | 模具和芯盒关系、名称快照完整复制 |
| MDM-048 | 铸造 BOM 工装 | 跨产品克隆 | 克隆到其他产品 | 不沿用来源产品工装；仅保留与目标产品匹配关系 |
| MDM-049 | 铸造 BOM 工装 | 删除约束 | 删除被 BOM 历史版本引用的模具或芯盒 | 后端拒绝删除并提示已被其他资料引用 |
| MDM-050 | 铸造 BOM 工装 | 领料边界 | 计算生产需求 | 返回工装摘要，但模具和芯盒不计入物理领料数量 |
| MDM-051 | 工序 | 工段字典 | 修改工序工段字典后新建工序 | 所属工段下拉使用 `operationSections` 真实数据 |
| MDM-052 | 工序 | 状态管理 | 禁用工序后查看历史路线，再重新启用 | 历史节点保留；禁用期间新路线不可选；启用后恢复可选 |
| MDM-053 | 工艺路线 | 多产品多设备 | 一条路线选择多产品/半成品，节点选择多设备 | 产品和设备都通过 Prisma relation 持久化，刷新不丢失 |
| MDM-054 | 工艺路线 | 图校验 | 分别提交自环、重复边、循环和孤立节点 | 后端拒绝非法图；草稿可未完结，发布必须是完整 DAG |
| MDM-055 | 工艺路线 | 汇合防错 | 浇注节点汇合多分支后发布 | 至少两个前置；强制炉批、铁水包和砂芯批次标志 |
| MDM-056 | 工艺路线 | 拓扑序号 | 保存多分支路线后查看详情 | 后端按拓扑序生成 `10/20/30...` 工序号 |
| MDM-057 | 工艺路线 | 版本接替 | 发布 V1.0，创建并发布 V2.0 | V2.0 生效，V1.0 自动停用，默认产品关系迁移 |
| MDM-058 | 工艺路线 | 产品路线唯一 | 已被草稿或生效路线占用的产品再关联到另一条路线 | 后端拒绝并提示产品编码及原路线；同一路线升版允许保留产品 |
| MDM-059 | 工艺路线 | 克隆 | 基于现有版本克隆新路线 | 新编码、V1.0 草稿，完整复制节点/设备/边，不复制适用产品和默认产品 |
| MDM-060 | 工艺路线权限 | 菜单、按钮与 API | 分别授予 view/create/edit/version/clone/activate/disable/default | 菜单、页头按钮、行操作和 API 均按实际授权生效 |
| MDM-061 | 工艺路线权限 | 越权 ID 访问 | 本人数据角色请求他人路线详情和状态操作 | 返回记录不存在，不泄露或修改越权数据 |
| MDM-062 | 工艺路线 | 列表状态保留 | 设置状态/产品/版本筛选，进入详情后返回 | URL 查询参数和列表筛选保持不变 |
| MDM-063 | 工艺路线适用产品 | 已生效直接维护 | 在已生效路线的“适用产品”标签页新增成品或半成品 | 关系立即持久化，路线版本和状态不变，刷新后仍存在 |
| MDM-064 | 工艺路线适用产品 | 移除与默认清理 | 移除当前以该路线为默认路线的产品 | 适用关系和 `ProductDefaultRouting` 在同一事务内删除 |
| MDM-065 | 工艺路线适用产品 | 类型与状态限制 | 尝试关联原材料，或修改已停用路线 | 后端分别拒绝非法物料类型和停用版本修改 |
| MDM-066 | 工艺路线适用产品 | 历史工单稳定 | 创建工单后从路线适用范围移除该产品 | 历史工单仍保留已锁定的路线版本和快照 |
| MDM-066A | 工艺路线适用产品 | 停用释放归属 | 停用原路线后把原产品关联到另一条路线 | 新路线可以保存；已停用历史版本仍保留原关联供追溯 |
| MDM-066B | 工艺路线回收站 | 回收与恢复 | 将已停用版本移入回收站，再执行恢复 | 普通列表隐藏、回收站可查看；恢复后仍为已停用并回到普通列表，历史关系不删除 |
| MDM-066C | 工艺路线回收站 | 状态与权限 | 对草稿/生效版本调用回收，或无 `model.routing.recycle` 权限调用回收/恢复 | 后端拒绝操作；前端不显示无权操作按钮 |
| MDM-066D | 工艺路线列表 | 去除材质与产品计数 | 打开工艺路线列表并调整适用产品 | 无材质牌号筛选和列；“关联产品数”与当前版本实际适用产品数量一致 |
| MDM-067 | 合炉排产 | 单炉容量单位 | 当前材质的已生效配方分别绑定启用的吨/炉和 kg/炉熔炼设备，打开目标熔炼设备下拉 | 两种单位的设备均可见，容量统一换算为 kg 参与排产计算 |
| MDM-068 | 合炉排产 | 设备选项范围 | 为当前材质准备已生效/草稿/已停用配方，并绑定启用/停用、容量有效/无效的设备 | 下拉仅显示该材质已生效配方绑定且已启用、单炉容量有效的设备 |
| MDM-069 | 合炉排产 | 无有效设备诊断 | 选择仅有草稿/已停用配方，或仅绑定停用/非法容量设备的材质 | 设备选项为空并统一显示“当前材质暂无可用熔炼设备，请检查已生效配方和设备容量配置” |
| MDM-070 | 合炉排产 | 未满炉容量计算 | 对容量 10000kg/炉的设备排产目标重量 6500kg | 剩余产能为 3500kg，利用率为 65%，允许生成合炉排产单 |
| MDM-071 | 合炉排产 | 满炉容量边界 | 对容量 10吨/炉的设备排产目标重量 10000kg | 剩余产能为 0kg，利用率为 100%，允许生成合炉排产单 |
| MDM-072 | 合炉排产 | 超容量双重防护 | 使目标重量超过设备单炉容量，再绕过页面直接调用生成接口 | 前端禁用生成操作并提示超容量；后端返回业务错误且不写入排产数据 |
| MDM-073 | 合炉排产 | 后端设备与班组校验 | 分别提交配方未绑定设备、停用设备、零/负数/不支持单位容量设备，以及不属于设备车间的班组 | 后端逐一拒绝并返回明确错误，不得仅依赖前端选项限制 |
| MDM-074 | 合炉排产 | 材质快速切换 | 在前一材质设备请求未返回时快速切换到另一材质 | 仅回写最后选中材质的设备选项，旧请求返回不覆盖新选项 |
| MDM-075 | 合炉排产 | 设备刷新失败 | 已加载设备后修改材质或手动刷新，并使最新设备请求失败 | 页面清除或标记旧设备为不可用，显示加载失败且旧数据不可提交 |
| MDM-076 | 合炉排产 | 同一工单并发分配 | 对同一工单剩余全部件数同时发起两次排产 | 一个请求成功，一个返回 `409`；最终有效分配件数等于且不超过原剩余件数 |
| MDM-077 | 合炉排产自动化 | 集成测试数据隔离 | 运行 `npm --prefix apps/api run test:production-execution` 并对比执行前后 `public` schema 业务数据 | 测试在独立临时 schema 中初始化、执行和清理，不新增、修改或删除 `public` 业务数据 |
| MDM-078 | 模具档案 | 一模多芯盒新增 | 新增一个模具并一次填写水道芯盒、曲轴箱芯盒、油道芯盒 | 模具与三套芯盒在同一事务保存，详情返回三条启用芯盒 |
| MDM-079 | 模具档案 | 多芯盒编辑与停用 | 修改已有芯盒、新增第四套，并从表单移除一条已保存芯盒 | 修改和新增成功；被移除的历史芯盒保留且状态变为停用 |
| MDM-080 | 模具档案 | 芯盒编码冲突回滚 | 为另一模具提交已被占用的芯盒编码 | 返回明确冲突错误，模具和所有芯盒写入整体回滚 |
| MDM-081 | 模具档案 | 开发单唯一建档 | 对同一已完成开发单连续创建两个模具档案 | 首次成功；第二次提示已建档，不覆盖原关联 |
| MDM-082 | 模具档案 | 来源开发单变更 | 编辑模具，把来源从开发单 A 改为未建档的已完成开发单 B | A 的归档模具清空，B 关联当前模具，模具字段同步更新 |
| MDM-083 | 模具/芯盒权限 | 嵌套操作独立授权 | 仅授予模具编辑，不授予芯盒新增/编辑，再打开模具编辑 | 可修改模具字段；芯盒只读；绕过前端提交嵌套修改返回 403 |
| MDM-084 | 模具/芯盒归属 | 嵌套归属原子性 | 使用本人数据范围账号新增模具和多套芯盒 | 模具与每条芯盒同时产生归属记录，创建人刷新后均可见 |
| MDM-085 | 铸造 BOM 工装 | 芯件比默认与维护 | 选择含三套启用芯盒的模具，分别把芯件比设为 2、4、1 | 自动带入三行且默认 1；修改后刷新仍保留对应比例 |
| MDM-086 | 铸造 BOM 工装 | 芯件比校验与兼容 | 分别提交 0、负数，以及旧版 `coreBoxCodes` 请求 | 非正数被拒绝；旧请求按芯件比 1 保存 |
| MDM-087 | 铸造 BOM 工装 | 版本复制与需求计算 | 创建新版本并按 100 件调用计算接口 | 新版本完整继承芯件比；比例为 4 的芯盒返回需求量 400 |
| MDM-088 | 芯盒档案 | 穴数新增与回填 | 分别从模具档案嵌套新增和芯盒档案独立新增，填写穴数 2、4 | 保存后详情和列表返回原穴数，编辑页面正确回填 |
| MDM-089 | 芯盒档案 | 穴数校验与兼容 | 分别提交 0、负数、小数、非数字，并提交未携带穴数的旧请求 | 非正整数被拒绝并提示穴数；旧请求按穴数 1 保存 |
| MDM-090 | 铸造 BOM 工装 | 芯盒保质期保存 | 为不同芯盒填写 8.5 小时、24 小时和空值后保存 | 刷新详情后分别返回 8.5、24、空值，芯件比不受影响 |
| MDM-091 | 铸造 BOM 工装 | 保质期校验 | 分别提交 0 和负数保质期 | 后端拒绝并提示保质期必须大于 0 小时 |
| MDM-092 | 铸造 BOM 工装 | 保质期版本继承 | 创建 BOM 新版本并调用生产需求计算接口 | 新版本和计算结果原样返回每套芯盒的保质期小时数 |

### 制芯自动化与手工用例

| 编号 | 方式 | 模块 | 用例 | 操作 | 预期 |
|---|---|---|---|---|---|
| MDM-093 | 自动化 | 制芯入口 | 路线节点决定生成入口 | 分别创建锁定路线含/不含 `operation.section=制芯` 的工单并读取详情 | 仅含制芯节点的工单返回 `requiresCoremaking=true`；无制芯节点不显示手动生成按钮并提示无需制芯 |
| MDM-094 | 自动化 | 制芯生成 | 一工单多芯盒与唯一约束 | 为含三套 BOM 芯盒的工单预览并手动生成任务，再重复提交同一芯盒及并发生成 | 首次按芯盒生成三条任务；同一 `workOrderId + coreBoxCode` 只能有一条，重复/并发请求一个成功、其余返回 `409` |
| MDM-095 | 自动化 | 制芯计算 | Decimal 需求量和压盒次数 | 运行 `test:coremaking-calculations`，覆盖 `100×2×1.03`、`3×1.5`、穴数 4/2 及四位小数和溢出边界 | 计划量分别向上取整为 206、5，206/4 压盒 52 次；浮点边界稳定，非法精度和溢出被拒绝 |
| MDM-096 | 自动化 | 制芯派工 | 节点、设备、车间、班组和版本校验 | 分别使用已绑定设备节点、无设备节点、停用设备/车间和跨车间班组生成或派工，再用旧 `versionNo` 重复提交 | 有绑定时严格限定节点设备；无绑定时回退启用射芯/制芯设备；设备或所属车间停用时在预览到开工各入口均不可用；非法组合被拒绝；完整派工进入 `WAITING`；旧版本返回 `409` |
| MDM-097 | 自动化 | 制芯报工 | 多次报工、唯一批次与原子流水 | 对同一 `IN_PROGRESS` 任务分两次报工并检查数据库，再并发提交相同旧版本 | 每次各生成一条 `CoreProductionReport`、唯一 `CoreInventoryBatch` 和 `PRODUCED` 流水；累计合格数达标后完成；并发仅一次成功且事务不留孤儿记录 |
| MDM-098 | 自动化 | 烘干/保质期 | 两种保质期起算时间 | 免烘干报工一批；需烘干报工一批后选择启用烘干设备确认 | 免烘干从 `reportedAt` 起算；需烘干先为 `UNDRIED` 且不可用，确认后从 `driedAt` 起算；每次动作递增 `versionNo` |
| MDM-099 | 自动化 | 临期/过期 | 24 小时边界与禁止领用 | 把批次失效时间置于 24 小时边界、边界外和当前时间，再调用刷新及 `validateCoreConsumption` | 剩余 `<=24h` 为 `WARNING` 且可领，`>24h` 为 `AVAILABLE`，到期为 `EXPIRED` 且 validate/consume 均拒绝 |
| MDM-100 | 自动化 | 砂芯库存 | 状态筛选、冻结、解冻、报废 | 查询分页库存，依次冻结、用旧版本重试、解冻、报废并查看详情流水 | 列表按状态/关键词分页；冻结/解冻数量不变并恢复实时状态；报废清零；流水含 `PRODUCED/LOCKED/UNLOCKED/SCRAPPED` |
| MDM-101 | 自动化 | 砂芯齐套 | 多芯盒需求、待烘干和缺口 | 调用 `/admin/production/work-orders/:id/core-readiness`，准备可用、临期、待烘干、过期、冻结和报废库存 | 只汇总 `AVAILABLE/WARNING`；逐芯盒返回需求、可用、待烘干、缺口、最短剩余时间及 `READY/PARTIAL/SHORTAGE` |
| MDM-102 | 自动化 | 跨 BOM 兼容 | 同产品跨 BOM 同芯盒使用 | 旧 BOM 工单生产同一芯盒批次，目标工单锁定新 BOM 且仍包含该芯盒；再换成不同产品或目标 BOM 不含该芯盒 | 同产品且目标锁定 BOM 含同芯盒时可计入齐套并通过消费校验；不同产品或目标 BOM 不含时拒绝 |
| MDM-103 | 自动化 | 未来造型服务 | 库存消费并发 | 对同一可用批次并发调用 `consumeCoreBatch`，请求总量超过库存 | 批次行锁、可串行化事务和条件更新阻止负库存；成功扣减各写一条 `CONSUMED` 流水，耗尽后状态为 `CONSUMED` |
| MDM-104 | 自动化 | 制芯权限 | 管理端权限键与尾斜杠 | 按 `view/create/dispatch/cancel/start/report/dry` 和库存 `view/dry/lock/scrap` 逐项访问接口，并对动作 URL 追加 `/` | 无最小动作权限返回 `403`；查看权限不隐含写操作；带或不带尾斜杠的权限结果一致 |
| MDM-105 | 自动化 | 班组隔离 | 小程序任务关系双重校验 | 给用户 `mini.production.core.view/start/report/dry`，分别加入任务班组、仅加入其他班组、移出班组后访问列表与已知 ID | 只有当前执行班组成员可见可操作；非成员列表不返回，详情/动作按不存在处理；超级管理员例外 |
| MDM-106 | 手工+自动化 | 小程序扫码/标签 | 混砂批次扫码与二维码标签 | 在报工页扫描 URL 编码、普通编码和畸形转义内容；从详情打开批次标签并重复生成二维码 | 扫码安全提取批次值且畸形内容不崩溃；标签使用后端 `qrContent` 本地生成稳定二维码，展示批次、芯盒、产品、数量、生产/烘干/失效信息 |
| MDM-107 | 自动化 | 请求竞态 | 旧响应和卸载页面隔离 | 快速切换任务状态/详情/库存标签，使旧请求后返回；动作请求未完成时卸载旧页并打开新页 | latest-request gate 只允许最新请求回写；旧响应不覆盖记录或 loading，卸载页不 `setData`，旧动作 `409` 不刷新新页面 |
| MDM-108 | 手工 | 管理端页面 | 路由、列表和打印标准 | 访问制芯任务列表/详情、砂芯库存和工单详情，筛选状态、拖列宽、触发超过 3 个授权操作并打印标签 | 路由受查看权限保护；列表使用 `ResizableTable`、固定右侧操作列和 `TableActions`；打印区域含真实二维码且不破坏现有图片、编码、标签页规则 |
| MDM-109 | 手工 | 一期范围 | 未来造型边界 | 检查菜单、路由和控制器，并尝试查找造型任务、下芯领用页面及 validate/consume HTTP 入口 | 仅齐套接口已暴露；领域服务存在，但一期无造型页面、造型任务、领用页面和 validate/consume HTTP 控制器，不出现假入口 |
| MDM-110 | 手工 | Docker 验收 | 本地容器端到端回归 | 由环境验收代理执行 `npm run docker:up`，访问 `http://localhost:8080` 和 `http://localhost:3000/api/health`，再运行三组制芯集成脚本 | 管理端/API/数据库健康；任务、报工、库存和齐套脚本通过且临时 schema 被清理；本次文档代理不启动或修改 Docker |

## 自动化验证点

- 所有新增接口必须返回统一 `{ code, message, data }`。
- 无权限用户访问建模接口必须返回 `403`。
- 不存在的外键引用必须返回 `400`。
- 多值字段必须从真实明细表映射回接口字段。
- 被引用主数据不能删除。
- 配方自动编码必须符合 `REC-YYYYMMDD-NNN`，并发冲突时自动重试。
- 配方状态与按钮显示必须同时由前端权限和后端状态机约束。
- BOM 状态与按钮显示必须同时由前端权限和后端状态机约束。
- BOM 生产计算结果必须引用明确的 `bomVersionId`；后续生产单必须保存版本和明细快照。
- 合炉排产必须统一换算吨/炉与 kg/炉容量，前后端共同校验配方绑定、设备状态、容量合法性、班组车间和超容量边界。
- 生产计算自动化命令：`npm --prefix apps/api run test:production-calculations`，覆盖容量单位换算、非法容量和分配计算边界。
- 生产执行自动化命令：`npm --prefix apps/api run test:production-execution`，必须使用独立临时 schema 并在结束时删除，不得改变 `public` schema 的业务数据。
- 自动化命令：`npm --prefix apps/api run test:casting-boms`，测试数据结束后必须自动清理。
- 多芯盒自动化命令：`npm --prefix apps/api run test:mold-coreboxes`，覆盖事务、归属、开发单唯一关联、冲突回滚、历史芯盒停用和旧请求兼容。
- 工序与路线自动化命令：`npm --prefix apps/api run test:process-routings`，覆盖图校验、多产品/多设备、已生效路线适用产品增减、停用只读、默认关系清理、版本、克隆和自动清理。
- 制芯纯计算命令：`npm --prefix apps/api run test:coremaking-calculations`，覆盖 Decimal 计划量、压盒次数、保质期起算和 24 小时边界。
- 制芯任务命令：`npm --prefix apps/api run test:coremaking-tasks`，覆盖生成入口、多芯盒拆分、任务快照、派工校验、唯一约束和并发 `409`。
- 制芯执行命令：`npm --prefix apps/api run test:coremaking-execution`，覆盖多次/并发报工、唯一批次、库存流水、烘干、临期/过期、冻结/解冻/报废、权限和班组隔离。
- 砂芯齐套命令：`npm --prefix apps/api run test:core-readiness`，覆盖齐套、跨工单/跨 BOM 同芯盒兼容、validate/consume 校验、并发消费和负库存防护。
- 管理端制芯命令：`node --test apps/admin/tests/coremaking-permissions.test.mjs apps/admin/tests/coremaking-ui.test.mjs`，覆盖菜单/路由/按钮权限、尾斜杠、latest-request、标签二维码和列表标准。
- 小程序制芯命令：`npm --prefix apps/miniprogram test`，覆盖真实 mini API、班组动作标志、扫码、二维码、`409` 原地刷新和旧响应隔离。
- `test:coremaking-tasks`、`test:coremaking-execution`、`test:core-readiness` 只允许本地 PostgreSQL 或显式授权环境，均使用独立临时 schema 并在结束时删除，不得改写 `public` 业务数据。
